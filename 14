#include <stdio.h>
int main() {
    float val, sum = 0;
    for (int i = 1; i <= 10; i++) {
        printf("Enter value %d: ", i);
        scanf("%f", &val);
        sum += val;
    }
    printf("Sum = %.2f\nMean = %.2f", sum, sum/10);
    return 0;
}
