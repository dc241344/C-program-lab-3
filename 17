#include <stdio.h>
int main() {
    int num, p=0, ne=0, z=0;
    for (int i = 1; i <= 200; i++) {
        scanf("%d", &num);
        if (num > 0) p++;
        else if (num < 0) ne++;
        else z++;
    }
    printf("+ve=%d -ve=%d zero=%d", p, ne, z);
    return 0;
}
